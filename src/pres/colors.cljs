(ns pres.colors)


;; add a common palette in case we need to change all of them quickly


(def highlight-fill "rgba(0,50,100, 0.05)")
(def highlight-stroke "rgba(0,50,100, 0.2)")
(def blur-fill "rgba(0,40,80,0.2)")
(def focus-fill "#333")
